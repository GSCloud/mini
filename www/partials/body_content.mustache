<style>
{{>body_css}}
{{>xbody_css_debug}}
</style>

<div class="container center">

{{^user.id}}
  <section class="login-section">
    {{>login}}
  </section>
{{/user.id}}

{{#user.id}}
  <section class="login-section">
    {{>logout}}
  </section>
{{/user.id}}

  <h1 class="hidden">{{ l.title }}</h1>
  <div id="fullscreen" class="right"><i class="material-icons">fullscreen</i> fullscreen</div>
  <h2>{{ l.title }}</h2>
</div>

<div class="container">
  <p>{{ l.readme }}</p>
</div>

<script>
(function (w, $, undefined) {
  'use strict';

  // fn GSC.FullScreen()
  if (typeof w.GSC.FullScreen != "function") w.GSC.FullScreen = function() {
    if (document.fullscreenElement) {
      document.exitFullscreen();
    } else {
      document.documentElement.requestFullscreen();
    }
  }

  // add nonce to all anchors
  $('.container').find('a').each(function() {
    var h = $(this).attr('href');
    var nonce = '?nonce=' + Math.round(Math.random() * 10000);
    if (h.indexOf('?') == -1) $(this).attr('href', h + nonce);
  });

  // action on fullscreen button
  $("#fullscreen").click(function () {
    GSC.FullScreen()
  });

})(window, $);

</script>